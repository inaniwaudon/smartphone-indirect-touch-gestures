export const MAX_TRIAL = Number(import.meta.env.VITE_MAX_TRIAL);
export const SCROLL_ROW_DISTANCE = Number(
	import.meta.env.VITE_SCROLL_ROW_DISTANCE,
);

export const DOUBLE_TAP_TIME_THRESHOLD = Number(
	import.meta.env.VITE_DOUBLE_TAP_TIME_THRESHOLD,
);
export const LONG_PRESS_TIME_THRESHOLD = Number(
	import.meta.env.VITE_LONG_PRESS_TIME_THRESHOLD,
);
export const DRAGGING_TIME_THRESHOLD = Number(
	import.meta.env.VITE_DRAGGING_TIME_THRESHOLD,
);

export const LONG_PRESS_RANGE_THRESHOLD = Number(
	import.meta.env.VITE_LONG_PRESS_RANGE_THRESHOLD,
);
